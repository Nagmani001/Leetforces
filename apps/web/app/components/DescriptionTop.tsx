export default function DesciptionTop() {
  return <div className="flex flex-col gap-y-2 pt-10 pl-5">
    <div>
      Given a number N and an array A of N numbers. Print the
      <span className="font-semibold">lowest number</span> and it's <span className="font-semibold">position</span>
    </div>
    <div>
      <span className="font-semibold">Note</span>:
      if there are more than one answer print first one's position.
    </div>
    <div className="font-semibold">Input</div>
    <div>
      First line contains a number N (2 ≤ N ≤ 1000) number of elements.
    </div>
    Second line contains N numbers (-105  ≤  Ai  ≤  105).<div>
    </div>

    <div className="font-semibold">Output</div>
    <div>Print the lowest number and its position (1-index).</div>

  </div>
}
